# Core
numpy>=1.26
python-dotenv>=1.0

# Embeddings (default: local sentence-transformers, no API key needed)
sentence-transformers>=3.0
torch>=2.2

# Optional: real OpenAI embeddings to match the paper's text-embedding-3-large
# openai>=1.40

# Optional: real Presidio (the synthetic eval uses a lookup detector by default)
# presidio-analyzer>=2.2
# presidio-anonymizer>=2.2
